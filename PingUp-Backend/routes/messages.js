const express = require('express');
const router = express.Router();
// Currently, there are no REST routes for messages.
// All message handling is done via WebSockets in sockets/handlers.js
// This file is created as a placeholder to satisfy the PR request.

module.exports = router;
